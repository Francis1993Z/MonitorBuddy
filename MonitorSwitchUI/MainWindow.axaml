<Window xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d" d:DesignWidth="420" d:DesignHeight="320"
        xmlns:local="clr-namespace:MonitorSwitch"
        x:Class="MonitorSwitch.MainWindow"
        Title="MonitorBuddy"
        Width="620" Height="850"
        CanResize="False"
        WindowStartupLocation="CenterScreen"
        ExtendClientAreaToDecorationsHint="True"
        ExtendClientAreaChromeHints="NoChrome"
        Background="Transparent">

    <Border CornerRadius="16"
            Background="#1E1E2E"
            BorderBrush="#313244"
            BorderThickness="1"
            Padding="24"
            ClipToBounds="True">

        <Grid RowDefinitions="Auto,Auto,Auto,Auto,*,Auto">

            <!-- Titre -->
            <TextBlock Grid.Row="0"
                       Text="MonitorBuddy"
                       FontSize="22" FontWeight="Bold"
                       Foreground="#CDD6F4"
                       HorizontalAlignment="Center"
                       Margin="0,4,0,20"/>

            <!-- Panneau diagnostic : écrans détectés (scrollable) -->
            <Border Grid.Row="1"
                    Background="#181825"
                    CornerRadius="8"
                    Padding="10"
                    Margin="0,0,0,16"
                    MaxHeight="350">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <TextBlock x:Name="DiagText"
                               Text="Détection en cours..."
                               FontSize="10.5"
                               FontFamily="Consolas, monospace"
                               Foreground="#A6ADC8"
                               TextWrapping="NoWrap"/>
                </ScrollViewer>
            </Border>

            <!-- Panneau assignation des rôles -->
            <Border Grid.Row="2"
                    Background="#181825"
                    CornerRadius="8"
                    Padding="10"
                    Margin="0,0,0,16"
                    MaxHeight="220">
                <StackPanel Spacing="8">
                    <TextBlock Text="Assignation des rôles"
                               FontSize="12" FontWeight="SemiBold"
                               Foreground="#CDD6F4"/>

                    <ScrollViewer VerticalScrollBarVisibility="Auto" MaxHeight="170">
                        <ItemsControl x:Name="RoleList">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate x:DataType="local:RoleItem">
                                    <Border Background="#313244" CornerRadius="6" Padding="8,5" Margin="0,2">
                                        <Grid ColumnDefinitions="Auto,*,Auto">
                                            <TextBlock Grid.Column="0"
                                                       Text="{Binding StatusIcon}"
                                                       FontSize="11" FontWeight="Bold"
                                                       Foreground="{Binding StatusColor}"
                                                       VerticalAlignment="Center"
                                                       Margin="0,0,8,0"
                                                       Width="28"/>
                                            <TextBlock Grid.Column="1"
                                                       Text="{Binding FriendlyName}"
                                                       FontSize="11.5"
                                                       Foreground="#CDD6F4"
                                                       VerticalAlignment="Center"
                                                       TextTrimming="CharacterEllipsis"/>
                                            <ComboBox Grid.Column="2"
                                                      SelectedIndex="{Binding RoleIndex}"
                                                      FontSize="11"
                                                      MinWidth="100"
                                                      Background="#45475A"
                                                      Foreground="#CDD6F4"
                                                      VerticalAlignment="Center">
                                                <ComboBoxItem Content="Ignoré"/>
                                                <ComboBoxItem Content="Bureau"/>
                                                <ComboBoxItem Content="TV"/>
                                            </ComboBox>
                                        </Grid>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </ScrollViewer>
                </StackPanel>
            </Border>

            <!-- Panneau configuration layout Bureau -->
            <Border Grid.Row="3"
                    Background="#181825"
                    CornerRadius="8"
                    Padding="10"
                    Margin="0,0,0,16">
                <StackPanel Spacing="8">
                    <TextBlock Text="Configuration Bureau (gauche → droite)"
                               FontSize="12" FontWeight="SemiBold"
                               Foreground="#CDD6F4"/>

                    <!-- Liste des écrans avec boutons -->
                    <ItemsControl x:Name="LayoutList">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate x:DataType="local:LayoutItem" x:CompileBindings="False">
                                <Border Background="#313244" CornerRadius="6" Padding="8,6" Margin="0,2">
                                    <Grid ColumnDefinitions="Auto,*,Auto,Auto">
                                        <TextBlock Grid.Column="0"
                                                   Text="{Binding Position}"
                                                   FontSize="12" FontWeight="Bold"
                                                   Foreground="#89B4FA"
                                                   VerticalAlignment="Center"
                                                   Margin="0,0,8,0"/>
                                        <TextBlock Grid.Column="1"
                                                   Text="{Binding Name}"
                                                   FontSize="12"
                                                   Foreground="#CDD6F4"
                                                   VerticalAlignment="Center"/>
                                        <Button Grid.Column="2"
                                                Tag="{Binding Name}"
                                                Click="OnClickSetPrimary"
                                                Background="Transparent"
                                                Foreground="{Binding PrimaryColor}"
                                                FontSize="16"
                                                Padding="4,2"
                                                Cursor="Hand"
                                                ToolTip.Tip="Définir comme principal"
                                                Content="★"/>
                                        <StackPanel Grid.Column="3" Orientation="Horizontal" Spacing="2">
                                            <Button Tag="{Binding Name}"
                                                    Click="OnClickMoveLeft"
                                                    Background="Transparent"
                                                    Foreground="#A6ADC8"
                                                    FontSize="14"
                                                    Padding="4,2"
                                                    Cursor="Hand"
                                                    Content="◀"/>
                                            <Button Tag="{Binding Name}"
                                                    Click="OnClickMoveRight"
                                                    Background="Transparent"
                                                    Foreground="#A6ADC8"
                                                    FontSize="14"
                                                    Padding="4,2"
                                                    Cursor="Hand"
                                                    Content="▶"/>
                                        </StackPanel>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </StackPanel>
            </Border>

            <!-- Boutons -->
            <StackPanel Grid.Row="4"
                        Spacing="16"
                        VerticalAlignment="Center">

                <!-- Bouton Mode TV -->
                <Button x:Name="BtnModeTV"
                        Click="OnClickModeTV"
                        HorizontalAlignment="Stretch"
                        HorizontalContentAlignment="Center"
                        Height="56"
                        CornerRadius="12"
                        Background="#45475A"
                        Foreground="#CDD6F4"
                        FontSize="16" FontWeight="SemiBold"
                        Cursor="Hand">
                    <StackPanel Orientation="Horizontal" Spacing="12">
                        <TextBlock Text="📺" FontSize="22" VerticalAlignment="Center"/>
                        <TextBlock Text="Mode TV" VerticalAlignment="Center"/>
                    </StackPanel>
                </Button>

                <!-- Bouton Mode Bureau -->
                <Button x:Name="BtnModeBureau"
                        Click="OnClickModeBureau"
                        HorizontalAlignment="Stretch"
                        HorizontalContentAlignment="Center"
                        Height="56"
                        CornerRadius="12"
                        Background="#45475A"
                        Foreground="#CDD6F4"
                        FontSize="16" FontWeight="SemiBold"
                        Cursor="Hand">
                    <StackPanel Orientation="Horizontal" Spacing="12">
                        <TextBlock Text="🖥️" FontSize="22" VerticalAlignment="Center"/>
                        <TextBlock Text="Mode Bureau" VerticalAlignment="Center"/>
                    </StackPanel>
                </Button>

            </StackPanel>

            <!-- Barre de statut + Quitter -->
            <Grid Grid.Row="5" Margin="0,16,0,0">
                <TextBlock x:Name="StatusText"
                           Text="Prêt"
                           FontSize="12"
                           Foreground="#6C7086"
                           HorizontalAlignment="Center"
                           VerticalAlignment="Center"/>

                <StackPanel Orientation="Horizontal" Spacing="8"
                            HorizontalAlignment="Right" VerticalAlignment="Center">
                    <Button Click="OnClickMinimiser"
                            Background="#45475A"
                            Foreground="#CDD6F4"
                            CornerRadius="8"
                            Padding="14,6"
                            FontSize="12" FontWeight="SemiBold"
                            Cursor="Hand"
                            Content="—  Minimiser"/>
                    <Button Click="OnClickQuitter"
                            Background="#F38BA8"
                            Foreground="#1E1E2E"
                            CornerRadius="8"
                            Padding="14,6"
                            FontSize="12" FontWeight="SemiBold"
                            Cursor="Hand"
                            Content="✕  Quitter"/>
                </StackPanel>
            </Grid>

        </Grid>
    </Border>
</Window>
